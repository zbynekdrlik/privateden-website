---
// Root page - redirects to detected browser language
---
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Privat Eden - Redirecting...</title>
    <script is:inline>
      (function() {
        var supported = ['sk', 'pl', 'en'];
        var defaultLang = 'sk';

        // 1. Check if user previously chose a language
        try {
          var saved = localStorage.getItem('preferred-lang');
          if (saved && supported.indexOf(saved) !== -1) {
            window.location.replace('/' + saved + '/');
            return;
          }
        } catch(e) {}

        // 2. Detect from browser language settings
        var langs = navigator.languages || [navigator.language || ''];
        var detected = defaultLang;

        for (var i = 0; i < langs.length; i++) {
          var code = langs[i].substring(0, 2).toLowerCase();
          if (supported.indexOf(code) !== -1) {
            detected = code;
            break;
          }
        }

        window.location.replace('/' + detected + '/');
      })();
    </script>
    <noscript>
      <meta http-equiv="refresh" content="0;url=/sk/" />
    </noscript>
  </head>
  <body>
    <p>Presmerovanie... / Redirecting... / Przekierowanie...</p>
  </body>
</html>
